from flask import Blueprint, request, jsonify, current_app
from backend.db_connection import get_db

artifacts = Blueprint("requests", __name__)

#GET /requests

#POST /requests

#PUT /requests

#DELETE /requests